import 'package:flutter/cupertino.dart';

class PrivacyPolicyContainer extends StatelessWidget {
  const PrivacyPolicyContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
